{
  "name": "roekoe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Roekoe — een online duivenmelker-managementspel voor vrienden (React client + Cloudflare Pages Functions + D1).",
  "scripts": {
    "install:all": "npm install && npm --prefix client install",
    "build": "npm --prefix client install && npm --prefix client run build",
    "dev:client": "npm --prefix client run dev",
    "dev": "npm run build && wrangler pages dev",
    "typecheck": "tsc --noEmit && npm --prefix client run typecheck",
    "db:migrate:local": "wrangler d1 migrations apply roekoe-db --local",
    "db:migrate": "wrangler d1 migrations apply roekoe-db --remote",
    "deploy": "npm run build && wrangler pages deploy client/dist"
  },
  "dependencies": {
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
